#Nodo

command {options} {arguments}

[[Listing files]]

ls = list
-l = large
-v= verbal
-r= reverse 
-t = timestamp
-S = size
pwd = Printing Working Directory
cd = changing directories
chmod = ==**ch**==ange the ==**mod**==es of access
chown = ==**ch**==ange ==**own**==er
cat = con==**cat**==enate
cp = copy files {OPTIONS} SOURCE DESTINATION
dd 
mv = move files
rm = remove files
grep = filtering input grep {OPTIONS} PATTERN {FILE}
ping -c {number} = ping with specific number of pings
ps = processes



ls = list
-l = largue
-v = verbal
-r = reverse
-t = timestamp
-S size
pwd = print working directory
cd = change directories
chmod ==**ch**==ange the ==**mod**==es of access
chown =  ==**ch**==ange ==**own**==er
cat = con==**cat**==enate
cp = copy files {OPTIONS} SOURCE DESTINATION
mv = move files
rm = remove files
grep = filtering input grep {OPTIONS} PATTERN {FILE}
ping -c {number} = ping with specific number of pings
ps = processes

ls = list
-l = large
-v = verbal
-r = reverse
-t timestamp
-S size
pwd = printing working directory
cd = change directory
chmod = ==**ch**==ange  the ==**mod**==es of access
chown = ==**ch**==ange ==**own**==er
cat = con==**cat**==enate
head = 10 firsts lines of the doc  -n = number of lines
tail = 10 last lines of the doc -n = number of lines
cp = copy files  {OPTIONS} SOURCE DESTINATION
mv= move files
rm = removing files
grep = filtering input grep {OPTIONS} PATTERN {FILE}
ping -c {number} = ping with specific number of pings
ps = processes


| d   | directory      |
| --- | -------------- |
| -   | regular file   |
| l   | Symbolic link  |
| s   | socket         |
| p   | pipe           |
| b   | block file     |
| c   | character life |

| d   | directory     |
| --- | ------------- |
| -   | regular file  |
| l   | symbolic link |
| s   | socket        |
| p   | pipe          |
| b   | block file    |
| c   | charac        |
